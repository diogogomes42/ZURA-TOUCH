# Zura Touch — Website

React + Vite marketing site for [Zura Touch](https://zuratouch.pt).

## Development

```bash
npm install
npm run dev
```

## Deploy

Configured for Vercel with **Root Directory** set to `SITE` in the parent repository.

```bash
npm run build
```

Output: `dist/`
